/*! Tailwind CSS Configuration Script */
(function() {
    'use strict';
    
    // Tailwind configuration (for runtime if using CDN in development only)
    if (typeof tailwind !== 'undefined') {
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: '#3B82F6',
                        secondary: '#10B981',
                        danger: '#EF4444'
                    }
                }
            }
        };
    }
    
    // Note: In production, Tailwind CSS should be compiled to static CSS
    // This file is a placeholder for development use only
    console.info('Tailwind CSS: Using compiled CSS for production');
})();